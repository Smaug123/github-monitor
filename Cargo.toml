[package]
name = "github-infra"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
serde_json = "1"
serde_yml = "0.0.12"
ureq = { version = "3", features = ["json"] }
base64 = "0.22"
regex = "1"

[dev-dependencies]
proptest = "1"
